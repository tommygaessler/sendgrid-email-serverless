{
  "name": "sendgrid-email-serverless",
  "version": "1.0.0",
  "description": "AWS Lambda Function that sends emails via sendgrid.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tommy Gaessler",
  "license": "ISC",
  "dependencies": {
    "sendgrid": "^4.7.1"
  }
}
